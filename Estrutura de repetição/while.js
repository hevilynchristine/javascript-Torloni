let volta = 0;  //inicialização

let opcaoEscolhida = parseInt(prompt("Digite 1 pra continuar"));

//condição 
while (opcaoEscolhida !== 1) {
   opcaoEscolhida = parseInt (prompt("Voce precisa escolher o número 1 para continuar"))

    //volta = volta + 1;
     //incrementa
}